class TopClassesModel {
  //top classes model class
  String title;
  String subTitle;
  String location;
  String image;
  bool isLiked = false;

  TopClassesModel(this.title, this.subTitle, this.location, this.image,this.isLiked);
}